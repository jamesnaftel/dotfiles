#"" bashrc
#

# Source dotfiles
for file in ~/.{exports,aliases,functions,prompts,sapfunctions,sapaliases,sapexports,bash_config}; do
	[ -r "$file" ] && [ -f "$file" ] && source "$file";
done;
unset file;

